# PR evidence — #633 twin-conflict annotations

Before/after screenshots for the cleaned-key twin-conflict annotations. The
"before" images are built from `main`; the "after" images from the feature
branch. Both are served from the same static shard build (`tw` field added on
the feature branch).

- `g6fmu-before-light.png` / `g6fmu-after-light.png` — G6FMU notes panel, light.
  Before: only the generic "listed more than once" cards. After: a leading
  derived note — "A non-standard spelling holds the active licence" — with the
  normality fact, the recency line (pool-row caveat, since the canonical form is
  undated), and an expanded "show the working" panel citing the snapshot and the
  ledger.
- `g6fmu-before-dark.png` / `g6fmu-after-dark.png` — the same, dark theme.
- `m7tee-after-light.png` / `m7tee-after-dark.png` — a no-conflict callsign,
  unchanged (no annotation, no manufactured doubt).
