# PR/issue evidence (consolidated)

One directory per PR/issue number (e.g. `129/before.png`). The historical
`meta/pr-evidence-<n>` branches were consolidated here as parents of this commit,
so every existing `raw.githubusercontent.com/<sha>/...` link still resolves.
