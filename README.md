# PR #610 evidence — one marker vocabulary

Before/after screenshots of the statistics page: derivation keeps codepoints ({U+XXXX}); the render layer shows friendly names ({NBSP}, {SP}, {ZWSP}) with the exact code point in the tooltip.
