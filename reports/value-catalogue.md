# Value catalogue

Every distinct value of the tracked fields across the whole corpus
(open-data publications + FOI observations), with counts and the lanes
that carry it. Regenerated by the normalise sweep and committed, so a
change here in a PR diff is a drift signal - a new status, a prefix that
should not exist, a fresh vocabulary variant. Values are the *derived /
canonical* surface; nothing is dropped or force-mapped - unmapped values
are shown, never assumed away.

## `status` — 2 distinct

| value | count | lanes |
|---|---:|---|
| `Allocated` | 1 | open-data |
| `Available` | 1 | open-data |

## `product / licence_class` — 2 distinct

| value | count | lanes |
|---|---:|---|
| `(blank)` | 1 | open-data |
| `Amateur Foundation Radio Licence` | 1 | open-data |

## `implied_class` — 2 distinct

| value | count | lanes |
|---|---:|---|
| `Foundation` | 1 | open-data |
| `Full` | 1 | open-data |

## `parse_status` — 1 distinct

| value | count | lanes |
|---|---:|---|
| `parsed` | 2 | open-data |

## `prefix_series` — 2 distinct

| value | count | lanes |
|---|---:|---|
| `G5` | 1 | open-data |
| `M7` | 1 | open-data |
